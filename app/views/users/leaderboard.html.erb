<style>
ul li:nth-child(even){
  background-color: #f5f5f5;
  font-size: 22px;
  font-family: 'Catamaran', sans-serif;}

ul li:nth-child(odd){
  font-size: 22px;
  font-family: 'Catamaran', sans-serif;}

p{
  float: right; margin-right: 3%; margin-bottom: 0px; margin-top: 0px; padding: 0px;
  font-size: 22px;}

li a{
  color: #417ddb;
}

</style>

<br>
<div class='row'>
  <!--<div class="large-2 columns"><h6></h6></div>-->
  <div class="large-4 columns large-centered">
		<h3 style="text-align: center;">Writer Leaderboard</h3>
		<ul>
				<% @users.each do |user|%>
				<li style="padding: 4px;"><%= link_to user.name, user_path(user) %><p><%= user.this_years_score %></p></li>
				<% end %>
		</ul>
		<%= will_paginate @posts, :class => 'flickr_pagination' %>
	</div>
<!--
  <div class="large-4 columns">
    <h3 class="itemheader">Top Posts</h3>
        <% Post.all.sort_by{|p| p.score}.reverse.first(5).each do |post| %>
        <img src=<%= post.full_size%> >
        <h6 class="sidebarposttitle"><%= link_to post.title, post_path(post) %></h6>
        <hr>
        <% end %>
  </div>
  <div class="large-2 columns"><h6></h6></div> -->
</div>
